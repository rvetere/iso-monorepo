{
  "name": "@nighttrax/foo",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build:package": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -b .",
    "prepublishOnly": "yarn run build:package",
    "test": "yarn run build:package"
  },
  "devDependencies": {
    "rimraf": "~3.0.2",
    "typescript": "~4.0.0"
  }
}
